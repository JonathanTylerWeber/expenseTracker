server:
  port: 8080 # set the port for spring app

spring:
  profiles:
    active: db

  datasource:
    # url: jdbc:h2:mem:expensedb;DB_CLOSE_DELAY=-1 # url for h2 db in memory mode
    # driver-class-name: org.h2.Driver # jdbc driver for h2
    # username: sa
    # password:
    url: jdbc:postgresql://ep-ancient-rice-a1fuboex-pooler.ap-southeast-1.aws.neon.tech/expensedb?sslmode=require
    driver-class-name: org.postgresql.Driver
    username: expensedb_owner
    password: npg_mYT0DeUGhcQ5

  h2:
    console:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: update # automatically creates or updates the db based on entity classes
    show-sql: true # enables logging of sql queries generated by jpa
    # database-platform: org.hibernate.dialect.H2Dialect
    database-platform: org.hibernate.dialect.PostgreSQLDialect
